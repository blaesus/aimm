daemon off;

events {
}

http {
    server {
        listen 2000;
        location / {
            proxy_pass http://localhost:3000;
        }
        location /api {
            proxy_pass http://localhost:4000;
        }
        location /admin-api {
            proxy_pass http://localhost:4100;
        }
    }
}
